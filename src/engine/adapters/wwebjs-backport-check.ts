/**
 * Legacy backport guard retained only as a placeholder for the now-removed adapter path.
 */
export const BACKPORT_MISSING_MESSAGE = 'The legacy whatsapp-web.js backport check is no longer used.';

export function isBackportMissing(): boolean {
  return false;
}
